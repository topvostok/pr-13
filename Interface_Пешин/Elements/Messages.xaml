<UserControl x:Class="Interface_Пешин.Elements.Messages"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400"
             MouseDoubleClick="DeleteMessage">

    <Border Background="#FF2D2D30" CornerRadius="10" Margin="5" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Изображение -->
            <Image x:Name="MessageImage" 
                   Grid.Row="0"
                   MaxWidth="300" 
                   MaxHeight="200" 
                   Stretch="Uniform"
                   Margin="0,0,0,5"
                   Visibility="Collapsed"/>

            <!-- Текст сообщения -->
            <TextBlock x:Name="MessageText" 
                      Grid.Row="1"
                      TextWrapping="Wrap" 
                      Foreground="White" 
                      FontSize="14"
                      Margin="0,0,0,5"/>

            <!-- Дата и время -->
            <TextBlock x:Name="MessageDate" 
                      Grid.Row="2"
                      TextWrapping="Wrap" 
                      Foreground="LightGray" 
                      FontSize="10"
                      HorizontalAlignment="Right"/>
        </Grid>
    </Border>
</UserControl>